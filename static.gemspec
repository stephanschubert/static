Gem::Specification.new do |s|
  s.name = "static"
  s.summary = "Rails 3 engine for static pages."
  s.description = "Rails 3 engine (w/ Mongoid) for supporting static pages"
  s.files = Dir["{config,lib}/**/*"] + ["MIT-LICENSE", "Rakefile"]
  s.version = "0.0.1"
end
